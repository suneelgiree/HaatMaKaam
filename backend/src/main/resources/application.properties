# ===============================================
# SPRING BOOT CONFIGURATION
# ===============================================
server.port=8080

# Activate the 'local' profile to load secrets from application-local.properties
spring.profiles.active=local

# ===============================================
# DATABASE CONFIGURATION (NON-SECRET)
# (Put only non-secret info here, like the database URL and username)
# ===============================================
spring.datasource.url=jdbc:postgresql:jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.tpdygdbzegjaxyddnbej
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect