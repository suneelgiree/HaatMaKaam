# ===================================================================
# POSTGRESQL DATABASE CONFIGURATION
# ===================================================================

# This is the JDBC URL for your Supabase database.
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require

# This is the driver class name for PostgreSQL.
spring.datasource.username=postgres.tpdygdbzegjaxyddnbej

# This is the password for your Supabase database.
spring.datasource.password=Suneel670@@

# ===================================================================
# JPA & HIBERNATE CONFIGURATION
# ===================================================================

# This setting validates the database schema against your code on startup.
# It's a safe setting for production that prevents accidental data loss.
spring.jpa.hibernate.ddl-auto=validate

# This makes the generated SQL queries visible in the console during development.
# It's very useful for debugging.
spring.jpa.show-sql=true

# This helps format the SQL logs to be more readable.
spring.jpa.properties.hibernate.format_sql=true

# Specifies the correct SQL dialect for PostgreSQL.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect